import 'package:flutter/material.dart';

class forvirate extends StatefulWidget {
  forvirate({Key? key}) : super(key: key);

  @override
  _forvirateState createState() => _forvirateState();
}

class _forvirateState extends State<forvirate> {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: double.infinity,
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [Text("صفحة عبدالشكور ")],
      ),
    );
  }
}
